export const useDeviceDimensions = () => {
    const fullHeight = window.innerHeight;
    const fullWidth = window.innerWidth;
    return { fullHeight, fullWidth };
};
//# sourceMappingURL=useDeviceDimensions.js.map